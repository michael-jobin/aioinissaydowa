import { defineConfig } from 'astro/config'

// https://astro.build/config
export default defineConfig({
 site: 'https://www.aioinissaydowa.co.jp/',
 base: '/humancapital/',
 // trailingSlash: "always"
})
